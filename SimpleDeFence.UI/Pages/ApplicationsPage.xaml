<Page
    x:Class="SimpleDeFence.UI.Pages.ApplicationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleDeFence.UI.Pages">

    <Grid Padding="28" RowSpacing="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="2">
            <TextBlock Text="Applications" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock x:Name="SummaryText" Style="{StaticResource BodyTextBlockStyle}" Opacity="0.8"
                       Text="Loading..."/>
        </StackPanel>

        <InfoBar Grid.Row="1" x:Name="Notice" IsOpen="False" IsClosable="True"/>

        <Grid Grid.Row="2" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="FilterBox" PlaceholderText="Filter by name or path"
                     TextChanged="FilterBox_TextChanged" MaxWidth="360" HorizontalAlignment="Left"/>
            <Button Grid.Column="1" x:Name="RefreshButton" Content="Refresh" Click="RefreshButton_Click"/>
            <ProgressRing Grid.Column="2" x:Name="Busy" IsActive="False" Width="20" Height="20"
                          VerticalAlignment="Center"/>
        </Grid>

        <ListView Grid.Row="3" x:Name="ExceptionList" SelectionMode="Single"
                  ScrollViewer.HorizontalScrollMode="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:ExceptionRow">
                    <Grid Padding="0,8" ColumnSpacing="14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220"/>
                            <ColumnDefinition Width="110"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="2">
                            <TextBlock Text="{x:Bind Name}" Style="{StaticResource BodyStrongTextBlockStyle}"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{x:Bind Created}" Style="{StaticResource CaptionTextBlockStyle}"
                                       Opacity="0.6"/>
                        </StackPanel>

                        <TextBlock Grid.Column="1" Text="{x:Bind Kind}" VerticalAlignment="Center"
                                   Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.8"/>

                        <TextBlock Grid.Column="2" Text="{x:Bind Detail}" VerticalAlignment="Center"
                                   Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.8"
                                   TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{x:Bind Detail}"/>

                        <Border Grid.Column="3" VerticalAlignment="Center" CornerRadius="4"
                                Padding="8,2" Background="{x:Bind PolicyBackground}">
                            <TextBlock Text="{x:Bind Policy}" Style="{StaticResource CaptionTextBlockStyle}"/>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
