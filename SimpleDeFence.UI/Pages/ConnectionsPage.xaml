<Page
    x:Class="SimpleDeFence.UI.Pages.ConnectionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleDeFence.UI.Pages"
    xmlns:loc="using:SimpleDeFence.UI.Localization">

    <Grid Padding="28" RowSpacing="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{loc:Loc Key=connections.title}" Style="{StaticResource TitleTextBlockStyle}"/>

        <InfoBar Grid.Row="1" x:Name="Notice" IsOpen="False" IsClosable="True"/>

        <Grid Grid.Row="2" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="FilterBox" PlaceholderText="{loc:Loc Key=connections.filterPlaceholder}"
                     TextChanged="FilterBox_TextChanged" MaxWidth="360" HorizontalAlignment="Left"/>
            <ToggleSwitch Grid.Column="1" x:Name="AutoRefreshToggle" OnContent="{loc:Loc Key=connections.autoRefresh}"
                          OffContent="{loc:Loc Key=connections.autoRefresh}" Toggled="AutoRefreshToggle_Toggled"/>
            <Button Grid.Column="2" x:Name="RefreshButton" Content="{loc:Loc Key=common.refresh}" Click="RefreshButton_Click"/>
            <ProgressRing Grid.Column="3" x:Name="Busy" IsActive="False" Width="20" Height="20" VerticalAlignment="Center"/>
        </Grid>

        <!-- Horizontal axis disabled for the same reason SettingsPage's is: the row templates
             below use fixed-width columns, so at narrow window sizes they measure wider than the
             viewport and would otherwise grow a sideways scroll instead of letting the cells trim. -->
        <ScrollViewer Grid.Row="3"
                      HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Spacing="12">
                <Expander x:Name="BlockedExpander" IsExpanded="True" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon FontSize="16" Glyph="&#xE733;"/>
                            <TextBlock x:Name="BlockedHeaderText" Style="{StaticResource SubtitleTextBlockStyle}"/>
                        </StackPanel>
                    </Expander.Header>
                    <StackPanel>
                        <TextBlock x:Name="BlockedEmpty" Text="{loc:Loc Key=connections.empty.blocked}"
                                   Style="{StaticResource BodyTextBlockStyle}" Opacity="0.7" Margin="0,8" Visibility="Collapsed"/>
                        <ListView x:Name="BlockedList" SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="local:BlockedListItem">
                                    <!-- TextWrapping="NoWrap": same starved-"*"-column defect
                                         RulesPage's row template documents at length - the Fluent
                                         TextBlock styles wrap by default and TextTrimming only trims
                                         the last line, so a narrow cell grows a tall ribbon and
                                         shoves the following rows out of view. -->
                                    <!-- Padding="12,8", where the other row templates use "0,6":
                                         this is the one row that paints its own background, and a
                                         tinted band whose fill starts exactly at its first glyph and
                                         ends exactly at its last button reads as a rendering seam
                                         rather than a deliberate callout. The extra inset is inside
                                         the fill, so the band still spans the full list width and
                                         stays aligned with the rows above and below it. -->
                                    <Grid Padding="12,8" ColumnSpacing="14"
                                          Background="{ThemeResource SystemFillColorCriticalBackgroundBrush}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="220"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <FontIcon Grid.Column="0" FontSize="14" Glyph="&#xE733;" VerticalAlignment="Center"
                                                  Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                                        <TextBlock Grid.Column="1" Text="{x:Bind AppName}" Style="{StaticResource BodyStrongTextBlockStyle}" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
                                        <TextBlock Grid.Column="2" Text="{x:Bind Detail}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.8" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
                                        <TextBlock Grid.Column="3" Text="{x:Bind When}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6" VerticalAlignment="Center" TextWrapping="NoWrap"/>
                                        <Button Grid.Column="4" Content="{loc:Loc Key=connections.allow}"
                                                Click="{x:Bind AllowButton_Click}"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Expander>

                <Expander x:Name="ConnectedExpander" IsExpanded="True" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon FontSize="16" Glyph="&#xE8AB;"/>
                            <TextBlock x:Name="ConnectedHeaderText" Style="{StaticResource SubtitleTextBlockStyle}"/>
                        </StackPanel>
                    </Expander.Header>
                    <StackPanel>
                        <TextBlock x:Name="ConnectedEmpty" Text="{loc:Loc Key=connections.empty.connected}"
                                   Style="{StaticResource BodyTextBlockStyle}" Opacity="0.7" Margin="0,8" Visibility="Collapsed"/>
                        <ListView x:Name="ConnectedList" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="local:ConnectionListItem">
                                <Grid Padding="0,6" ColumnSpacing="14">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="220"/>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{x:Bind AppName}" Style="{StaticResource BodyStrongTextBlockStyle}" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
                                    <TextBlock Grid.Column="1" Text="{x:Bind Protocol}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.8" TextWrapping="NoWrap"/>
                                    <TextBlock Grid.Column="2" Text="{x:Bind RemoteEndpoint}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.8" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
                                    <TextBlock Grid.Column="3" Text="{x:Bind State}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.8" TextWrapping="NoWrap"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Expander>

                <Expander x:Name="OpenExpander" IsExpanded="True" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon FontSize="16" Glyph="&#xE704;"/>
                            <TextBlock x:Name="OpenHeaderText" Style="{StaticResource SubtitleTextBlockStyle}"/>
                        </StackPanel>
                    </Expander.Header>
                    <StackPanel>
                        <TextBlock x:Name="OpenEmpty" Text="{loc:Loc Key=connections.empty.open}"
                                   Style="{StaticResource BodyTextBlockStyle}" Opacity="0.7" Margin="0,8" Visibility="Collapsed"/>
                        <ListView x:Name="OpenList" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="local:ConnectionListItem">
                                <Grid Padding="0,6" ColumnSpacing="14">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="220"/>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{x:Bind AppName}" Style="{StaticResource BodyStrongTextBlockStyle}" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
                                    <TextBlock Grid.Column="1" Text="{x:Bind Protocol}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.8" TextWrapping="NoWrap"/>
                                    <TextBlock Grid.Column="2" Text="{x:Bind LocalEndpoint}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.8" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Expander>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
